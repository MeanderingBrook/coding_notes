extends layout 

block content 
  h1= title 

  if language_list.length 
    ul 
      each language in language_list 
        li 
          a(href=language.url) #{language.name}

  else 
    p There are no Languages.
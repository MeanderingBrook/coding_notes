extends layout 

block content 
  h1= title 

  if component_list.length 
    ul 
      each component in component_list 
        li 
          a(href=component.url) #{component.name}

  else 
    p There are no Components.
      
